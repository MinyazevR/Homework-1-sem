#pragma once
#include <stdbool.h>

// Function to check the function that adds an element to the top of the stack
bool pushTest();

// Function to check the function that removes from the top of the stack
bool popTest();

// Function for checking a function that removes all stack elements
bool deleteStackTest();